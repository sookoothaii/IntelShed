fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
socksio>=0.2.0
websockets>=12.0
pydantic>=2.9.0
python-multipart>=0.0.12
beautifulsoup4>=4.12.0
dnspython>=2.6.0
gtfs-realtime-bindings>=1.0.0
protobuf>=4.0.0
river>=0.21.0
duckdb>=1.5.4
# Note: R-Tree index on GEOMETRY column is disabled on 1.5.x due to
# duckdb-spatial #769. The geom column + ST_Within are used without R-Tree.
# B-Tree index on lat/lon provides sufficient performance for <100k entities.
# R-Tree auto-enable will be re-evaluated when 1.6.0 is available on PyPI.
sqlite-vec>=0.1.9

# K4 — Satellite Imagery Change Detection (Sentinel-2 COG window-read via GDAL wheels)
rasterio>=1.3.0

# MCP read tools (Cursor / Claude) — Streamable HTTP at /api/mcp
mcp>=1.27,<2

# Intelligence layer: canonical entity schema (FollowTheMoney) on DuckDB.
# NOTE (Windows): followthemoney -> normality -> pyicu has no PyPI wheel for
# Windows. Install a prebuilt PyICU wheel first, e.g.:
#   pip install https://github.com/cgohlke/pyicu-build/releases/download/v2.16.2/pyicu-2.16.2-cp312-cp312-win_amd64.whl
# On Linux/Pi: apt-get install -y libicu-dev pkg-config  (then pip builds it).
followthemoney>=4.9
# tzdata: Windows ships no system zoneinfo DB; rigour/followthemoney need it.
tzdata>=2024.1

# Security & Rate Limiting
slowapi>=0.1.9
redis>=5.0.0
# Celery task queue + Flower monitoring (WORLDBASE_TASK_QUEUE=celery)
celery>=5.5,<6
flower>=2.0,<3
# stem: Tor control-port library for darkweb OPSEC exit-node rotation (Phase 3.2).
# Optional — darkweb_tor.py lazy-imports it and is a no-op when absent.
# Only needed with WORLDBASE_DARKWEB_TOR_ROTATE_IDENTITY=1.
stem>=1.8.2

# Database (PostgreSQL support)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Environment variables
python-dotenv>=1.0.0

# Document export (briefing PDF/DOCX/PPTX, feed status XLSX)
reportlab>=5.0
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1

# Optional: document intel ingest (GLiNER NER; GLiREL relations opt-in).
# PC-only / GPU — NOT on the Raspberry Pi. Lazy import: backend runs without these.
# License: see THIRD_PARTY_NOTICES.md — GLiREL is NOT in the default install.
#
# OSS-safe install (entities + mentions edges):
#   pip install "torch>=2.6" --index-url https://download.pytorch.org/whl/cu124
#   pip install gliner pdfplumber mail-parser
#   pip install "transformers>=4.51.3,<5" "huggingface_hub<1.0"
#
# Optional semantic relations (CC BY-NC-SA — personal/non-commercial opt-in only):
#   set WORLDBASE_INTEL_GLIREL=1 in backend/.env
#   pip install glirel loguru
#
# Optional entity resolution (Splink -> sameAs edges with confidence):
#   pip install "splink>=4.0,<5"
#   POST /api/intel/resolution/run  (or WORLDBASE_ENTITY_RESOLUTION_AUTOPILOT=1)
#
# Windows: import pyarrow before torch (handled in intel_ingest.py).
# torch >= 2.6 required for GLiREL .bin load (CVE-2025-32434).
#
# gliner>=0.2.27
# transformers>=4.51.3,<5
# huggingface_hub<1.0
# pdfplumber>=0.11
# mail-parser>=4.0
# glirel>=1.2.1          # optional — NOT default; see THIRD_PARTY_NOTICES.md
# loguru>=0.7            # only if glirel installed
#
# Optional RAG reranker (Track R0.1 + I7 ONNX quantization — CPU, 0 VRAM):
#   set RAG_RERANK=1 in backend/.env
#   pip install onnxruntime optimum sentence-transformers
# onnxruntime>=1.16
# optimum>=2.0
# sentence-transformers>=3.0

# Optional: V4-19 Predictive Analytics (LightGBM forecasting on snapshot time series)
#   set WORLDBASE_PREDICTIVE=1 in backend/.env
#   pip install lightgbm numpy
# lightgbm>=4.0
# numpy>=1.26

# Optional: V4-23 Anomaly Detection (Isolation Forest on feed time series)
#   set WORLDBASE_ANOMALY_DETECTION=1 in backend/.env
#   Falls back to z-score statistics when sklearn is not installed
scikit-learn>=1.3
